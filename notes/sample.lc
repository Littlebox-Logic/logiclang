#!/usr/bin/logicc

// This is a sample source file of Logic language.

header logic.io;
header logic.sys;

call logic.io;

@entry::thread(0)
global int entry(list<str> args)
{
	conoutln("Hello world.");
	ret 0;
}
